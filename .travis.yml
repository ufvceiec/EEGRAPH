language: python
os:
  - linux
python:
  - "3.7"
  - "3.8"
install:
  - pip install -r requirements.txt
  - pip install coverage
script: 
  - coverage run -m unittest tests.tests_basic
after_success:
  - bash <(curl -s https://codecov.io/bash)