numpy>=1.22
pandas>=1.3
mne>=1.2
networkx>=2.5
plotly>=5.0
scipy>=1.7
numba>=0.60    # 0.60+ supports NumPy 2.x; required by antropy
antropy>=0.1.4
kaleido==0.2.1

# scot 0.2.1 is broken with scipy >= 1.10 (uses removed scipy.shape).
# Install only if you need the DTF connectivity measure:
#   pip install scot==0.2.1 "scipy<1.10"

# Documentation
mkdocs>=1.5
mkdocs-material>=9.0
mkdocstrings[python]>=0.20
pymdown-extensions>=10.0
